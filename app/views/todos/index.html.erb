<ul class="todos">
	<% @todo.each do |todo| %>
		<li class="<%= "completed" if todo.completed? %>">
			<%= todo.title %>
			<%= button_to "Mark as completed", todo_completion_path(todo) %>
		</li>
	<% end %>
</ul>

<%= link_to "Add a new todo", new_todo_path %>
